version: '3.8'

services:
  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Erwin23154
      POSTGRES_DB: video_calls_and_conferences_app_database
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/video_calls_and_confereces_app_database.sql  # Скрипт для инициализации базы данных

volumes:
  pg_data: